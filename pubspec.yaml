name: myimage
description: "A new Flutter plugin project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.1
  flutter: ">=3.3.0"

dependencies:
  cunning_document_scanner: any
  dio: any
  flutter:
    sdk: flutter
  image_picker: any
  logger: any
  permission_handler: any
  provider: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any
